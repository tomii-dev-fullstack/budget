<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <title>
    <%= proposal.title %>
  </title>
  <style>
    body {
      font-family: Arial, sans-serif;
      color: #333;
    }

    .container {
      padding-left: 40px;
      padding-right: 40px;
    }

    h1,
    h2 {
      color: #222;
      margin-bottom: 10px;
    }

    p {
      margin: 4px 0;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    td,
    th {
      border: 1px solid #ddd;
      padding: 8px;
    }

    th {
      background-color: #f2f2f2;
    }

    .right {
      text-align: right;
    }

    .section {
      margin-top: 30px;
    }

    .signature {
      margin-top: 60px;
    }
  </style>
</head>

<body>
  <div class="container">

    <h1>
      <%= proposal.title %>
    </h1>

    <div class="section">
      <h2>Datos del Cliente</h2>
      <p><strong>Nombre:</strong>
        <%= proposal.client %>
      </p>
      <p><strong>Email:</strong>
        <%= proposal.email %>
      </p>
      <p><strong>Teléfono:</strong>
        <%= proposal.phone %>
      </p>
      <p><strong>Fecha de entrega:</strong>
        <%= proposal.date %>
      </p>
    </div>

    <div class="section">
      <h2>Resumen del Proyecto</h2>
      <p>
        <%= proposal.summary %>
      </p>
    </div>

    <div class="section">
      <h2>TYC del Proyecto</h2>
      <p>
        <%= proposal.terms %>
      </p>
    </div>

    <div class="section">
      <h2>Items</h2>
      <table>
        <thead>
          <tr>
            <th>Descripción</th>
            <th class="right">Monto</th>
          </tr>
        </thead>
        <tbody>
          <% proposal.items.forEach(item=> { %>
            <tr>
              <td>
                <%= item.description %>
              </td>
              <td class="right">$<%= item.amount.toFixed(2) %>
              </td>
            </tr>
            <% }) %>
              <tr>
                <th>Total</th>
                <th class="right">$<%= proposal.items.reduce((acc, item)=> acc + item.amount, 0).toFixed(2) %></th>
              </tr>
        </tbody>
      </table>
    </div>

    <div class="section">
      <h2>Términos y Condiciones</h2>
      <p>
        <%= proposal.terms %>
      </p>
    </div>

    <div class="signature">
      <p>Firma del cliente: __________________________</p>
    </div>
  </div>
</body>

</html>